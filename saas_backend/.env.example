# Production Port (Usually set by host like Railway/Render)
PORT=3000

# Environment (development | production)
NODE_ENV=production

# Stripe Secret Key (Placeholder for future payments)
STRIPE_SECRET_KEY=sk_test_...

# License Key Salt (Optional, if you sign keys)
LICENSE_SECRET=my_top_secret_salt
